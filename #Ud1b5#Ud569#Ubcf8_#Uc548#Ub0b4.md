# 몸가짐 CMS 통합본 — 한 번에 교체용

이 zip은 지금까지의 모든 수정이 반영된 전체 프로젝트입니다.
GitHub 저장소를 이 내용으로 통째로 교체하면 누락 없이 최신 상태가 됩니다.
(node_modules, dist는 빠져 있음 — Cloudflare가 자동 생성)

## ⚠️ 올리는 법 (Clean Replace · 반드시 이 순서로)

단순 덮어쓰기(Overwrite)를 하면 과거에 쓰이던 안 쓰는 파일이 남아
import(참조) 오류를 일으킬 수 있습니다. 아래처럼 **기존 폴더를 먼저 삭제**하세요.

1. 저장소에서 **기존 `src` 폴더를 통째로 삭제**(파일 일부만 지우지 말고 폴더째 삭제).
2. 이 zip 안의 **새 `src` 폴더를 그대로 붙여넣기**.
3. `package.json`, `firestore.rules`도 이 zip의 것으로 교체(있으면 덮어쓰기, 없으면 추가).
4. 커밋·푸시 → Cloudflare가 자동 빌드.
5. 배포 후 Cloudflare가 **초록 체크(성공)**인지, 그리고 **배포 시각이 방금 푸시한 시각**인지 확인.
   (배포 지연으로 옛 화면이 보일 수 있음 — 시각부터 확인)

> 요약: "기존 src 삭제 → 새 src 붙여넣기" (덮어쓰기 ❌)

## 🔒 운영 배포 전 필수: Firestore 보안 규칙 적용

`firestore.rules` 파일이 새로 들어 있습니다. **반드시 콘솔에 게시**해야 합니다.

- Firebase 콘솔 → Firestore Database → **규칙(Rules)** 탭 → `firestore.rules` 내용 붙여넣기 → **게시**.
- 핵심: `roles` 컬렉션은 클라이언트 **쓰기 전면 금지** → 권한 탈취 불가.
- 매출/지출/정산/설정은 **admin만** 읽기·쓰기.
- 더 강한 방식(Custom Claims)은 `CUSTOM_CLAIMS_가이드.md` 참고.

## 이 통합본의 변경 (이번 보안·안정화 패치)

### 1. 배포 가이드 — Clean Replace
- 위 "올리는 법"을 덮어쓰기 → 폴더 삭제 후 교체 방식으로 변경.

### 2. Firestore Batch 500건 한계 대응 (Chunking)
- `purgeMember` 등 대량 삭제가 **450건 단위로 자동 분할** 처리됨.
- 장기 회원(예약·수납·신체·AI 합산 500건 초과)도 중단 없이 파기 가능.
- 회원 문서는 **맨 마지막에 삭제** → 중간 실패 시 회원이 남아 **재실행하면 멱등하게 마저 삭제**.
- 최초 시드도 동일하게 분할 처리.

### 3. 인증·보안 규칙 강화
- `firestore.rules` 추가(roles write 금지, admin 전용 컬렉션 분리).
- 로그인 시 **Custom Claims(token.admin) 우선 → 없으면 roles 문서 폴백**으로 역할 결정.
- Custom Claims 전환 가이드(`CUSTOM_CLAIMS_가이드.md`) 동봉.

### 4. 핵심 테스트 확장 (8개 → 16개)
- 예약 삭제 시 세션 복원 / 삭제 실패 롤백 / 출석확정 후 비복원
- 600건 수납 회원 청크 파기 / 하위 삭제 실패 시 회원 보존
- 권한 가드: 비로그인·트레이너·관리자 접근 분기 검증

## 검증
- `npm install && npm run build`  → 빌드 성공
- `npm test`                      → **16개 테스트 통과**

## 아직 남은 권장 작업
- (권장) 의존성 취약점 업데이트, 번들 코드 분할(현재 500kB 경고)
- (권장) Custom Claims로 완전 전환 후 보안 규칙 `isAdmin()`을 token 기반으로 교체
