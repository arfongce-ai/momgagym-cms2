# 몸가짐 CMS 통합본 — 한 번에 교체용

이 zip은 지금까지의 모든 수정이 반영된 전체 프로젝트입니다.
GitHub 저장소를 이 내용으로 통째로 교체하면 누락 없이 최신 상태가 됩니다.
(node_modules, dist는 빠져 있음 — Cloudflare가 자동 생성)

## 올리는 법 (권장)
- 저장소의 src 폴더와 package.json을 이 zip의 것으로 통째 교체.
- 배포 후 Cloudflare가 초록 체크(성공)인지 확인.

## 이 통합본에 들어있는 변경 (그동안 작업 전부)

### AI 측정 (역도·1RM·VBT)
- 바벨 끝·원판 탭 추적 + 다중 추적점(최대 3개) + 중앙값 오차보정 + 신뢰도 표시
- 추적 떨림 보정(EMA)·데드존, 1RM 카메라 검은화면·색 오인식 수정
- 원판 무게 기록(색 인식 보조), 촬영 위치·거리 안내(측정 전 + 실시간)
- 카메라 전체화면(.measure-camera 공용 클래스)
- 자세·체형 측정: 메뉴에서 제거(재설계 예정), 내용 비움

### 데이터 정합성 (교차검증 1·2차 대응)
- 저장/삭제 실패가 화면에 전달됨(async+await, 실패 시 알림, 성공 후에만 갱신)
- AI 저장/삭제 Promise 오인 수정
- 회원 파기 시 AI 기록까지 원자적 삭제(purgeMember)
- 예약 생성+세션차감, 상태확정+세션복원을 batch로 원자적 처리
- 백업이 Firestore에서 수납·신체·AI 수집 + 항목 수 표시
- 설정 안내 정정, 무효 초기화 버튼 제거
- 회귀 테스트 포함(npm test → 8개 통과)

### 인증·로그인
- Firebase Authentication 로그인(관리자) + roles 문서로 권한 판단
- 평문 비밀번호 전부 제거(코드·공지)
- 통합 로그인 화면(관리자·트레이너 같은 화면에서 이메일 로그인)
- 로그인 유지(브라우저 지속성 명시 — 새로고침·재방문 유지)

## 검증
- npm install && npm run build  → 빌드 성공
- npm test                      → 8개 테스트 통과

## 아직 남은 필수 작업 (다음 단계)
- Firestore 보안 규칙 (콘솔 설정) — 운영 배포 전 마지막 관문
- (권장) 의존성 취약점 업데이트, 번들 코드 분할

## 추가 수정 (이번 버전 — 카메라 검은화면·1RM 작동 대응)
- **1RM 카메라 검은화면 해결**: 카메라 시작 시 영상 요소가 화면에 렌더될 때까지 대기 시간을 늘리고(최대 약 2초), 해상도(메타데이터)가 준비된 뒤 재생하도록 보강. 영상 요소를 끝내 못 잡으면 조용히 닫히지 않고 "다시 시도" 안내가 뜸.
- **1RM 카메라 닫기 정리**: 원판 인식 없이 카메라를 닫으면 자동으로 "무게 직접 입력" 모드로 복귀.
- VBT·역도·1RM 모두 동일한 풀스크린 오버레이(CameraStage) + 키 [적용] 버튼(HeightField) 통일 UX 확인.
- npm run build 성공 / npm test 8개 통과 재확인.

### 배포 후 점검 (실기기)
- 1RM → "원판 색 인식(보조)" → 카메라 시작 시 영상이 뜨는지.
- 안 뜨면: ① https 접속 여부 ② cdn.jsdelivr.net 차단 여부 ③ 카메라 권한 허용 여부 확인.
