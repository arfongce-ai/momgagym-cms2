# AGENTS.md — Codex / Claude Code 공통 협업 규칙

이 저장소는 Codex와 Claude Code가 번갈아 작업합니다.
저장소가 공개(public) 이므로 이 파일과 docs/HANDOFF.md에는 비밀값·개인정보를 쓰지 않습니다.

## 시작할 때
- docs/HANDOFF.md를 끝까지 읽는다
- git status, git branch --show-current로 미커밋 변경과 브랜치를 확인한다.
- HANDOFF의 작업 잠금 표에 내 도구와 수정할 파일을 적는다.
- 다른 도구의 잠금이 걸린 파일은 수정하지 않는다.

## 작업 중
- 기존 기능 위에 추가한다. 대체하거나 병행 구현하지 않는다.
- 큰 파일(예: App.jsx)은 전체 덮어쓰기 금지한다.
- 남의 미커밋 변경을 되돌리거나 덮어쓰지 않는다.
- 히스토리 재작성과 파일 영구삭제는 사용자 확인 없이 하지 않는다.
- 테스트 실패는 기준선과 비교해 새로 생긴 실패만 보고한다.

## 끝날 때
- 테스트와 빌드를 실행하고 결과를 기록한다.
- docs/HANDOFF.md의 작업 잠금에서 내 줄을 지우고 작업 로그에 항목을 추가한다.
- 변경을 커밋한다. 푸시는 사용자가 정한 방식대로 한다.

## 상호 검토 (교차 검토)
작업이 끝나면 다른 도구가 검토하고 보완한다. 절차는 docs/REVIEW_LOOP.md를 따른다.
검토자는 수정 전에 발견 내용을 HANDOFF에 먼저 기록하고, 테스트를 직접 다시 실행한다.

## 금지 (공개 저장소)
- API 키, 시크릿, 토큰, .env 값, Firebase 서비스 계정
- 회원 이름, 전화번호, 주소, 결제정보, 원본 음식 사진
- CMS로 보내는 영양 데이터는 가명 memberId 기준의 최소 필드만 사용한다.

## 응답 스타일
- 한국어, 짧고 직접적으로.
- 수정 위치는 파일:줄로 적고, 다음에 할 일을 구체적으로 적는다.
